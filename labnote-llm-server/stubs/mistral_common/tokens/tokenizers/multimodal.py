# Placeholder statistics used by the upstream script when handling multimodal
# tokenizers. The exact numeric values are irrelevant for standard text-only
# models, so we supply sensible defaults to keep the interface compatible.
DATASET_MEAN = (0.0, 0.0, 0.0)
DATASET_STD = (1.0, 1.0, 1.0)

